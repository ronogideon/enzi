# API base for the admin portal (build-time fallback).
# In production you can instead inject window.__ENV__.API_URL at container
# start via gen-env.mjs (reads API_URL) — no rebuild needed per environment.
VITE_API_URL="http://localhost:4000/api"

# Backend API. The trailing "/api" is added automatically if omitted.
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
# Storefront canonical URL (used for metadata)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
# WhatsApp number for the floating chat / contact CTA (no +)
NEXT_PUBLIC_WHATSAPP="254110050620"
